spring:
  config:
    import: optional:configserver:http://localhost:8888
    enabled: true
  application:
    name: cloudconfig
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      token: hvs.NdnU9y1h91vr9a6REv86m0L8
    
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
       