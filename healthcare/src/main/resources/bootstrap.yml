spring:
  config:
    import: optional:configserver:http://localhost:8888
   
  application:
    name: healthcare
  cloud:
    vault:
      host: 127.0.0.1
      port: 8200
      scheme: http
      authentication: TOKEN
      token: hvs.A0KdE71SBonRzva0AGRtXdv4
  
    
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
       